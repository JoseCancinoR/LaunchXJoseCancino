# Crear env -- python3 -m venv env
# Activar env -- Entrar a la carpeta del entorno vitual (env) ingresar el siguiente comando: .\Scripts\activate
# Desactivar env -- deactivate

""" from datetime import *
from dateutil.relativedelta import *
now = datetime.now()
print(now)

now = now + relativedelta(months=1, weeks=1, hour=10)

print(now) """
